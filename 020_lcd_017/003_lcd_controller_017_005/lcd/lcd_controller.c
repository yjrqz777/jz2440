

//向上接收不同lcd参数
//向下发送不同lcd参数 到对应lcd


void lcd_controller_init(p_lcd_params plcdparams)
{

    lcd_controller.init(plcdparams);


    //1.根据传入的参数，初始化lcd控制器
    //2.根据传入的参数，初始化lcd屏幕
    
    //3.根据传入的参数，设置lcd的参数
    //4.根据传入的参数，设置lcd的起始地址
    //5.根据传入的参数，设置lcd的结束地址
    //6.根据传入的参数，设置lcd的行数
    //7.根据传入的参数，设置lcd的列数
    //8.根据传入的参数，设置lcd的像素宽度
    //9.根据传入的参数，设置lcd的像素高度
    //10.根据传入的参数，设置lcd的像素位数
    //11.根据传入的参数，设置lcd的帧频率
    //12.根据传入的参数，设置lcd的时钟频率
    //13.根据传入的参数，设置lcd的数据极性
    //14.根据传入的参数，设置lcd的控制极性
    //15.根据传入的参数，设置lcd的使能极性
    //16.根据传入的参数，设置lcd的背光极性
    //17.根据传入的参数，设置lcd的数据端口
    //18.根据传入的参数，设置lcd的控制端口
    //19.根据传入的参数，设置lcd的使能端口
    //20.根据传入的参数，设置lcd的背光端口
    //21.根据传入的参数，设置lcd的背光端口
    //22.根据传入的参数，设置lcd的背光端口
    //23.根据传入的参数，设置lcd的背光端口
    //24.根据传入的参数，设置lcd的背光端口
    //25.根据传入的参数，设置lcd的背光端口





